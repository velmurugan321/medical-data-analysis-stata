# Medical Data Analysis Frontend

Planned React + TypeScript dashboard for the medical analysis platform.

## User flow
Upload Data → Variable View → Data Quality → Descriptive → Association → Regression → Diagnostic → ROC → Graphs → Tables → Export.

## Design rule
The frontend is responsible for interaction, configuration, validation feedback and visualization. It does not independently calculate statistical results; it consumes structured results from the backend API.
