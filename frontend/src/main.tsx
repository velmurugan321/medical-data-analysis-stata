import { useEffect, useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import './styles.css'

type Module = 'dashboard'|'upload'|'variables'|'quality'|'descriptive'|'table1'|'association'|'regression'|'diagnostic'|'roc'|'graphs'|'results'
type Info = { filename?: string; rows:number; columns:number; variables:Array<Record<string,unknown>> }
type ApiResponse = Record<string, any>
const API='https://medical-data-analysis-api.onrender.com'
const modules:[Module,string,string][]=[['dashboard','Dashboard','⌂'],['upload','Data Upload','↑'],['variables','Variable View','☷'],['quality','Data Quality','✓'],['descriptive','Descriptive','▤'],['table1','Table 1','▥'],['association','Association','↔'],['regression','Regression','β'],['diagnostic','Diagnostic','⊕'],['roc','ROC Analysis','⌁'],['graphs','Graphs','◒'],['results','Results & Export','↓']]

function App(){
 const [active,setActive]=useState<Module>('dashboard'),[file,setFile]=useState<File|null>(null),[info,setInfo]=useState<Info|null>(null),[quality,setQuality]=useState<any>(null),[result,setResult]=useState<ApiResponse|null>(null),[busy,setBusy]=useState(false),[message,setMessage]=useState('Upload a dataset to begin'),[apiStatus,setApiStatus]=useState<'ok'|'error'|'checking'>('checking')
 useEffect(()=>{fetch(`${API}/health`).then(r=>{if(!r.ok)throw Error();setApiStatus('ok')}).catch(()=>setApiStatus('error'))},[])
 const variables=useMemo(()=>info?.variables.map(v=>String(v.name))||[],[info])
 async function post(path:string,fields:Record<string,string>={}){if(!file)throw Error('Please upload a dataset first.');const f=new FormData();f.append('file',file);Object.entries(fields).forEach(([k,v])=>f.append(k,v));const r=await fetch(`${API}${path}`,{method:'POST',body:f});const text=await r.text();if(!r.ok)throw Error(text||`Request failed (${r.status})`);return JSON.parse(text)}
 async function loadDataset(next:File){setFile(next);setBusy(true);setMessage('Reading dataset and checking quality…');try{const f=new FormData();f.append('file',next);const r=await fetch(`${API}/api/v1/data/inspect`,{method:'POST',body:f});if(!r.ok)throw Error(await r.text());const i=await r.json();setInfo(i);const qf=new FormData();qf.append('file',next);const q=await fetch(`${API}/api/v1/data/quality`,{method:'POST',body:qf});setQuality(q.ok?(await q.json()).results:null);setResult(null);setMessage('Dataset is ready for analysis');setActive('dashboard')}catch(e){setMessage(e instanceof Error?e.message:'Upload failed')}finally{setBusy(false)}}
 async function run(path:string,fields:Record<string,string>){setBusy(true);setMessage('Running analysis…');try{setResult(await post(path,fields));setActive('results');setMessage('Analysis completed successfully')}catch(e){setResult({error:e instanceof Error?e.message:'Analysis failed'});setActive('results');setMessage('Analysis failed — review the message below')}finally{setBusy(false)}}
 return <div className="app">
  <aside className="sidebar"><div className="brand"><div className="brand-mark">M</div><div><strong>Medical Analytics</strong><span>Research statistics platform</span></div></div><div className="side-section">WORKSPACE</div><nav>{modules.map(([id,label,icon])=><button key={id} className={active===id?'active':''} onClick={()=>setActive(id)}><i>{icon}</i><span>{label}</span></button>)}</nav><div className="side-footer"><span className={`dot ${apiStatus==='error'?'bad':''}`}/>{apiStatus==='ok'?'Analysis engine online':apiStatus==='error'?'Analysis engine offline':'Connecting…'}<small>Python statistical engine</small></div></aside>
  <main className="main"><header><div><span className="eyebrow">Clinical research workspace</span><h1>{modules.find(x=>x[0]===active)?.[1]}</h1><p>{file?`${file.name}  ·  ${info?.rows??'—'} records  ·  ${info?.columns??'—'} variables`:'A clean workflow for medical data analysis'}</p></div><label className="top-upload">+ Upload dataset<input hidden type="file" accept=".csv,.xlsx,.xls,.dta,.tsv" onChange={e=>e.target.files?.[0]&&loadDataset(e.target.files[0])}/></label></header>
  <div className={`statusbar ${apiStatus}`}><span className="status-dot"/><b>{apiStatus==='ok'?'Engine ready':apiStatus==='error'?'Engine unavailable':'Checking engine'}</b><span>{message}</span></div>
  {active==='dashboard'&&<Dashboard info={info} busy={busy} onUpload={()=>setActive('upload')} onRun={()=>setActive('descriptive')} onModule={setActive}/>} 
  {active==='upload'&&<Upload file={file} busy={busy} onUpload={loadDataset}/>} 
  {active==='variables'&&<Panel title="Variable View" subtitle="Understand every variable before selecting an analysis.">{info?<VariableTable variables={info.variables}/>:<Empty icon="☷" title="No dataset loaded" text="Upload a CSV, Excel, Stata or TSV file to inspect variables."/>}</Panel>}
  {active==='quality'&&<Quality data={quality}/>} 
  {active==='descriptive'&&<VariablePicker title="Descriptive Statistics" variables={variables} hint="Choose one or more variables. Nothing is selected by default, so the analysis only runs on variables you explicitly choose." action="Run descriptive analysis" busy={busy} onRun={v=>v.length?run('/api/v1/analysis/descriptive',{variables:v.join(',')}):setMessage('Select at least one variable before running the analysis')}/>} 
  {active==='table1'&&<TableOne variables={variables} busy={busy} onRun={(v)=>v.variables.length?run('/api/v1/analysis/table-one',{variables:v.variables.join(','),group:v.group}):setMessage('Select at least one variable for Table 1')}/>} 
  {active==='association'&&<PairAnalysis title="Categorical Association" subtitle="Compare two categorical variables using chi-square / Fisher exact." a="Exposure / variable" b="Outcome" variables={variables} busy={busy} onRun={v=>run('/api/v1/analysis/association',{variable:v.a,outcome:v.b})}/>} 
  {active==='diagnostic'&&<PairAnalysis title="Diagnostic Accuracy" subtitle="Compare an index test against the reference standard." a="Index test" b="Reference standard" variables={variables} busy={busy} onRun={v=>run('/api/v1/analysis/diagnostic',{index_test:v.a,reference_standard:v.b})}/>} 
  {active==='roc'&&<PairAnalysis title="ROC Analysis" subtitle="Evaluate discrimination and identify the Youden-optimal threshold." a="Test / predictor" b="Binary outcome" variables={variables} busy={busy} onRun={v=>run('/api/v1/analysis/roc',{test:v.a,outcome:v.b})}/>} 
  {active==='regression'&&<Regression variables={variables} busy={busy} onRun={(path,c)=>run(path,{config:JSON.stringify(c)})}/>} 
  {active==='graphs'&&<Panel title="Publication-ready graphs" subtitle="The graph workspace is reserved for validated clinical visualizations."><Empty icon="◒" title="Graph workspace" text="Charts will be generated from selected analysis results rather than raw code output."/></Panel>}
  {active==='results'&&<Results result={result}/>}</main></div>
}

function Dashboard({info,busy,onUpload,onRun,onModule}:{info:Info|null;busy:boolean;onUpload:()=>void;onRun:()=>void;onModule:(m:Module)=>void}){return <div className="dashboard">
 <section className="hero-new"><div className="hero-copy"><div className="hero-badge"><span/> MEDICAL DATA ANALYSIS</div><h2>From raw dataset to <em>research-ready</em> results.</h2><p>Upload your clinical data, inspect variables, choose an analysis visually, and get clean statistical tables without seeing statistical code.</p><div className="hero-actions"><button className="primary" onClick={onUpload}>Upload dataset <span>→</span></button>{info&&<button className="ghost" onClick={onRun}>Start analysis</button>}</div><div className="trust-row"><span>✓ CSV</span><span>✓ Excel</span><span>✓ Stata</span><span>✓ TSV</span></div></div><div className="hero-visual"><div className="visual-window"><div className="window-top"><span/><span/><span/><b>Analysis workspace</b></div><div className="mini-chart"><div className="chart-bars"><i style={{height:'42%'}}/><i style={{height:'68%'}}/><i style={{height:'54%'}}/><i style={{height:'86%'}}/><i style={{height:'63%'}}/></div><div className="chart-line"><span>RR</span><b>1.42</b><small>95% CI 1.08–1.87</small></div></div><div className="mini-table"><span/><span/><span/><span/><span/><span/></div></div></div></section>
 <div className="stat-grid"><Stat icon="◫" label="Dataset" value={info?.filename||'Not loaded'} /><Stat icon="#" label="Records" value={info?info.rows.toLocaleString():'—'}/><Stat icon="☷" label="Variables" value={info?String(info.columns):'—'}/><Stat icon="✓" label="Engine" value={apiLabel()}/></div>
 <section className="section-block"><div className="section-heading"><div><span className="eyebrow">WORKFLOW</span><h3>Everything you need in one workspace</h3></div><span className="section-note">Built for medical research</span></div><div className="module-grid">{[['quality','Data Quality','Missing data, duplicates & consistency','✓'],['descriptive','Descriptive','Mean, SD, median & distributions','▤'],['table1','Table 1','Publication-style baseline table','▥'],['association','Association','Chi-square & Fisher exact','↔'],['regression','Regression','RR and OR models','β'],['diagnostic','Diagnostic','Sensitivity, specificity & CI','⊕']].map(([id,t,d,i])=><button className="module-card" key={id} onClick={()=>onModule(id as Module)}><span className="module-icon">{i}</span><div><b>{t}</b><small>{d}</small></div><strong>→</strong></button>)}</div></section>
 <section className="bottom-callout"><div><span className="eyebrow">SIMPLE BY DESIGN</span><h3>Select → Run → Review</h3><p>No statistical syntax required. Your data stays in the analysis workflow while results are presented as readable research output.</p></div><button className="secondary" onClick={onUpload}>Begin with a dataset</button></section>
 </div>}
function apiLabel(){return 'Python / FastAPI'}
function Stat({icon,label,value}:{icon:string;label:string;value:string|number}){return <div className="stat-card"><span className="stat-icon">{icon}</span><div><small>{label}</small><b title={String(value)}>{value}</b></div></div>}
function Upload({file,busy,onUpload}:{file:File|null;busy:boolean;onUpload:(f:File)=>void}){return <Panel title="Upload Dataset" subtitle="Supported: CSV, XLSX, XLS, Stata DTA and TSV."><label className="drop"><div className="drop-icon">↑</div><b>{busy?'Processing dataset…':'Drop your dataset here'}</b><span>or click to browse files</span><input type="file" accept=".csv,.xlsx,.xls,.dta,.tsv" onChange={e=>e.target.files?.[0]&&onUpload(e.target.files[0])}/></label>{file&&<div className="file-row"><span className="file-icon">✓</span><div><b>{file.name}</b><small>Dataset loaded and ready</small></div><em>{(file.size/1024).toFixed(1)} KB</em></div>}</Panel>}
function VariableTable({variables}:{variables:Array<Record<string,unknown>>}){return <div className="table-wrap"><table><thead><tr>{['Variable','Type','Storage','N','Missing','Unique','Min','Max'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{variables.map(v=><tr key={String(v.name)}><td><b>{String(v.name)}</b></td><td><span className="type-pill">{String(v.type)}</span></td><td>{String(v.storage_type)}</td><td>{String(v.n)}</td><td>{String(v.missing)}</td><td>{String(v.unique)}</td><td>{v.min==null?'—':String(v.min)}</td><td>{v.max==null?'—':String(v.max)}</td></tr>)}</tbody></table></div>}
function VariablePicker({title,variables,hint,action,busy,onRun}:{title:string;variables:string[];hint:string;action:string;busy:boolean;onRun:(v:string[])=>void}){const [selected,setSelected]=useState<string[]>([]);const all=variables.length>0&&selected.length===variables.length;const toggle=(v:string)=>setSelected(s=>s.includes(v)?s.filter(x=>x!==v):[...s,v]);return <Panel title={title} subtitle={hint}><SelectionToolbar count={selected.length} all={all} onAll={()=>setSelected(all?[]:variables)}/><div className="variable-grid">{variables.map(v=><button type="button" key={v} className={`variable-chip ${selected.includes(v)?'selected':''}`} onClick={()=>toggle(v)}><span className="check">{selected.includes(v)?'✓':''}</span><span>{v}</span></button>)}</div>{!variables.length&&<Empty icon="☷" title="No variables available" text="Upload a dataset before selecting variables."/>}<div className="action-row"><button className="primary" disabled={busy||!selected.length} onClick={()=>onRun(selected)}>{busy?'Running…':action}<span>→</span></button></div></Panel>}
function SelectionToolbar({count,all,onAll}:{count:number;all:boolean;onAll:()=>void}){return <div className="selection-toolbar"><div><b>{count}</b><span> variable{count===1?'':'s'} selected</span></div><button className="link" onClick={onAll}>{all?'Clear all':'Select all'}</button></div>}
function TableOne({variables,busy,onRun}:{variables:string[];busy:boolean;onRun:(v:{variables:string[];group:string})=>void}){const [selected,setSelected]=useState<string[]>([]),[group,setGroup]=useState('');const all=variables.length>0&&selected.length===variables.length;const toggle=(v:string)=>setSelected(s=>s.includes(v)?s.filter(x=>x!==v):[...s,v]);return <Panel title="Table 1" subtitle="Build a baseline characteristics table with an optional grouping variable."><SelectionToolbar count={selected.length} all={all} onAll={()=>setSelected(all?[]:variables)}/><div className="variable-grid">{variables.map(v=><button type="button" key={v} className={`variable-chip ${selected.includes(v)?'selected':''}`} onClick={()=>toggle(v)}><span className="check">{selected.includes(v)?'✓':''}</span>{v}</button>)}</div><div className="field"><span>Grouping variable <small>(optional)</small></span><select value={group} onChange={e=>setGroup(e.target.value)}><option value="">Overall only</option>{variables.map(v=><option key={v}>{v}</option>)}</select></div><button className="primary" disabled={busy||!selected.length} onClick={()=>onRun({variables:selected,group})}>{busy?'Generating…':'Generate Table 1'}<span>→</span></button></Panel>}
function PairAnalysis({title,subtitle,a,b,variables,busy,onRun}:{title:string;subtitle:string;a:string;b:string;variables:string[];busy:boolean;onRun:(v:{a:string;b:string})=>void}){const [x,setX]=useState(''),[y,setY]=useState('');return <Panel title={title} subtitle={subtitle}><div className="selection-grid"><SelectBox label={a} value={x} set={setX} variables={variables}/><SelectBox label={b} value={y} set={setY} variables={variables}/></div>{x&&y&&x===y&&<div className="warning">Choose two different variables for this analysis.</div>}<button className="primary" disabled={busy||!x||!y||x===y} onClick={()=>onRun({a:x,b:y})}>{busy?'Running…':'Run analysis'}<span>→</span></button></Panel>}
function SelectBox({label,value,set,variables}:{label:string;value:string;set:(v:string)=>void;variables:string[]}){return <label className="field"><span>{label}</span><select value={value} onChange={e=>set(e.target.value)}><option value="">Choose a variable…</option>{variables.map(v=><option key={v}>{v}</option>)}</select></label>}
function Regression({variables,busy,onRun}:{variables:string[];busy:boolean;onRun:(path:string,c:Record<string,unknown>)=>void}){const [model,setModel]=useState('poisson'),[outcome,setOutcome]=useState(''),[predictors,setPredictors]=useState<string[]>([]),[cat,setCat]=useState<string[]>([]),[refs,setRefs]=useState<Record<string,string>>({});const predictorVars=variables.filter(v=>v!==outcome);const toggle=(v:string)=>setPredictors(s=>s.includes(v)?s.filter(x=>x!==v):[...s,v]);const toggleCat=(v:string)=>setCat(s=>s.includes(v)?s.filter(x=>x!==v):[...s,v]);return <Panel title="Regression" subtitle="Configure the outcome, predictors, categorical variables and reference categories before running the model."><div className="selection-grid"><SelectBox label="Outcome (binary 0/1)" value={outcome} set={v=>{setOutcome(v);setPredictors(p=>p.filter(x=>x!==v));setCat(c=>c.filter(x=>x!==v));}} variables={variables}/><label className="field"><span>Model</span><select value={model} onChange={e=>setModel(e.target.value)}><option value="poisson">Robust Poisson — Risk Ratio</option><option value="logistic">Logistic — Odds Ratio</option></select></label></div><div className="selection-block"><SelectionToolbar count={predictors.length} all={predictorVars.length>0&&predictors.length===predictorVars.length} onAll={()=>setPredictors(predictors.length===predictorVars.length?[]:predictorVars)}/><div className="variable-grid compact">{predictorVars.map(v=><button type="button" key={v} className={`variable-chip ${predictors.includes(v)?'selected':''}`} onClick={()=>toggle(v)}><span className="check">{predictors.includes(v)?'✓':''}</span>{v}</button>)}</div></div>{predictors.length>0&&<div className="selection-block"><div className="block-title"><b>Categorical predictors</b><span>Select only variables that represent categories.</span></div><div className="variable-grid compact">{predictors.map(v=><button type="button" key={v} className={`variable-chip ${cat.includes(v)?'selected':''}`} onClick={()=>toggleCat(v)}><span className="check">{cat.includes(v)?'✓':''}</span>{v}</button>)}</div></div>}{cat.length>0&&<div className="reference-panel"><div className="block-title"><b>Reference categories</b><span>Choose the comparison baseline for each categorical predictor.</span></div>{cat.map(v=><label className="field" key={v}><span>{v} reference</span><select value={refs[v]||''} onChange={e=>setRefs(r=>({...r,[v]:e.target.value}))}><option value="">Automatic first observed level</option><option value="0">0</option><option value="1">1</option><option value="2">2</option><option value="No">No</option><option value="Yes">Yes</option><option value="Negative">Negative</option><option value="Positive">Positive</option></select></label>)}</div>}<button className="primary" disabled={busy||!outcome||!predictors.length} onClick={()=>onRun(model==='poisson'?'/api/v1/analysis/poisson':'/api/v1/analysis/logistic',{outcome,predictors,categorical_predictors:cat,reference_categories:refs})}>{busy?'Running model…':`Run ${model==='poisson'?'RR':'OR'} model`}<span>→</span></button></Panel>}
function Quality({data}:{data:any}){if(!data)return <Panel title="Data Quality" subtitle="Automatic checks for missingness, duplicates and variable issues."><Empty icon="✓" title="No dataset loaded" text="Upload a dataset to start quality checks."/></Panel>;const numeric=Object.entries(data).filter(([,v])=>typeof v==='number');return <Panel title="Data Quality" subtitle="Automatic checks completed when the dataset was uploaded."><div className="quality-cards">{numeric.slice(0,6).map(([k,v])=><Stat key={k} icon="✓" label={pretty(k)} value={String(v)}/>)}</div><HumanResult data={data}/></Panel>}
function Results({result}:{result:ApiResponse|null}){return <Panel title="Analysis Results" subtitle="Readable research output. Raw JSON is available only as an optional download."><div className="result-toolbar"><span>{result?.error?'Analysis needs attention':'Results ready for review'}</span><button className="secondary" disabled={!result} onClick={()=>download(result)}>Download JSON</button></div>{result?<HumanResult data={result}/>:<Empty icon="↓" title="No results yet" text="Run an analysis to generate results here."/>}</Panel>}
function HumanResult({data}:{data:any}){if(data?.error)return <div className="error-box">{String(data.error)}</div>;const payload=data?.results??data;if(Array.isArray(payload))return <SimpleTable rows={payload}/>;if(payload&&typeof payload==='object'){const entries=Object.entries(payload);const scalar=entries.filter(([,v])=>v==null||['string','number','boolean'].includes(typeof v));return <>{scalar.length>0&&<div className="metric-list">{scalar.map(([k,v])=><div className="metric" key={k}><span>{pretty(k)}</span><b>{format(v)}</b></div>)}</div>}<ComplexTables data={payload}/></>}return <div className="metric-list"><div className="metric"><span>Result</span><b>{format(payload)}</b></div></div>}
function ComplexTables({data}:{data:any}){return <>{Object.entries(data||{}).filter(([,v])=>Array.isArray(v)&&v.length&&typeof v[0]==='object').map(([k,v])=><div className="result-table" key={k}><h3>{pretty(k)}</h3><SimpleTable rows={v as any[]}/></div>)}</>}
function SimpleTable({rows}:{rows:any[]}){if(!rows.length)return null;const keys=Array.from(new Set(rows.flatMap(r=>typeof r==='object'&&r?Object.keys(r):['value'])));return <div className="table-wrap"><table><thead><tr>{keys.map(k=><th key={k}>{pretty(k)}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{keys.map(k=><td key={k}>{format(r?.[k])}</td>)}</tr>)}</tbody></table></div>}
function Panel({title,subtitle,children}:{title:string;subtitle?:string;children:any}){return <section className="panel"><div className="panel-heading"><div><h2>{title}</h2>{subtitle&&<p>{subtitle}</p>}</div></div>{children}</section>}
function Empty({icon='·',title,text}:{icon?:string;title:string;text:string}){return <div className="empty"><div className="empty-icon">{icon}</div><b>{title}</b><span>{text}</span></div>}
function pretty(k:string){return k.replaceAll('_',' ').replace(/\b\w/g,m=>m.toUpperCase())}
function format(v:any){if(v===null||v===undefined)return '—';if(typeof v==='number'){if(!Number.isFinite(v))return 'Not estimable';return Math.abs(v)<0.001&&v!==0?v.toExponential(2):Number.isInteger(v)?String(v):v.toFixed(3)}if(typeof v==='boolean')return v?'Yes':'No';if(Array.isArray(v))return v.map(format).join(', ');if(typeof v==='object')return Object.entries(v).map(([k,x])=>`${pretty(k)}: ${format(x)}`).join(' · ');return String(v)}
function download(data:any){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='medical-analysis-results.json';a.click();URL.revokeObjectURL(a.href)}
createRoot(document.getElementById('root')!).render(<App/>)